// Wrapper to catch asynchronous errors and pass them to Express next()
const catchAsync = (fn) => {
  return (req, res, next) => {
    fn(req, res, next).catch(next);
  };
};

module.exports = catchAsync;
